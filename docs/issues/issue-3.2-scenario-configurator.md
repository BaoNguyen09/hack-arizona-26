# Issue 3.2: Build Scenario Configurator & UI Controls

## Scope

- Add user inputs for technology type, capacity, CAPEX overrides, carbon price, and weights
- Add a time slider to animate cost surface shifts across hours or seasons

## Acceptance Criteria

- Controls update application state predictably
- UI defaults are sensible for the demo path
- Time controls are designed so they can degrade gracefully if full animation data is not ready
